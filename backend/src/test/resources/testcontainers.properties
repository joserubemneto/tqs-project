# Testcontainers configuration
# See: https://java.testcontainers.org/features/configuration/

# Disable Ryuk in CI environments (handled by ryuk.container.privileged)
# ryuk.disabled=false

# Disable checks that may fail with newer Docker API versions
checks.disable=true

# Connection timeout settings for CI environments
# docker.client.strategy=org.testcontainers.dockerclient.UnixSocketClientProviderStrategy
